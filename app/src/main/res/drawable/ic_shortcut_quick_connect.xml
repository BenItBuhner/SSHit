<?xml version="1.0" encoding="utf-8"?>
<!-- Quick connect's shortcut icon: the launcher's graphite with the prompt glyph, so it sits beside
     the host swatches as the one shortcut that is not a host. -->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background" />
    <foreground android:drawable="@drawable/ic_shortcut_quick_connect_foreground" />
</adaptive-icon>
