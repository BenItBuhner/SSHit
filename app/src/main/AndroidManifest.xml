<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".BerthApp"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting"
        tools:targetApi="37">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- ssh://user@host:port and sftp://user@host:port/path from browsers, mail and other
                 apps; read in onResume and acted on by the view model (SshLink, AppViewModel.openLink). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ssh" />
                <data android:scheme="sftp" />
            </intent-filter>
            <!-- Share to Berth (spec C24): a file, or several, dropped into the active session's /tmp
                 through the transfer queue, each path pasted as its copy lands; shared text with no
                 file behind it is pasted as typed. Read in onResume like a link, acted on once the
                 lock allows (IntentInbox, AppViewModel.arrive). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <!-- The launcher's shortcuts (spec Part B, App shortcuts): Quick connect is static, here; the
                 four recent hosts are published by LauncherShortcuts as the host list changes. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- The lock screen's window, over MainActivity in the same task while the app is locked: a
             window of its own is what covers the main window's sheets, menus and dialogs. Same theme
             minus the launch splash, with no transition either way. -->
        <activity
            android:name=".LockActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.App.Lock"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|uiMode" />

        <!-- Hands a downloaded file to the share sheet without copying it anywhere the user can see. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/shared_files" />
        </provider>

        <!-- Keeps SSH sockets alive while the app is in the background; there is no better-fitting
             foreground service type for an interactive remote shell. -->
        <service
            android:name=".session.SessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Interactive SSH terminal sessions the user started and expects to stay connected" />
        </service>
    </application>
</manifest>
